{
  "name": "eld-trip-planner",
  "version": "1.0.0",
  "private": true,
  "description": "E2E test harness for the ELD trip planner (Next.js frontend + Django backend)",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
